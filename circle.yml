machine:
  environment: 
    TOX_ENV: docs
    TOX_ENV: flake8
    TOX_ENV: py27
  python:
    version: 2.7.9

dependencies:
  override:
    - pip install tox
test:
  override:
    - tox -e $TOX_ENV

# notify:
#   flowdock: 1f4ec6febcf1ac9b35ae6c1f0049471f
