tosca_definitions_version: cloudify_dsl_1_1

node_types:
  cloudify.nodes.Compute:
    properties:
      install_agent:
        default: true

node_templates:
  node:
    type: cloudify.nodes.Compute

