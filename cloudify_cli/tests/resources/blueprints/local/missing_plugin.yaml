tosca_definitions_version: cloudify_dsl_1_0

plugins:
    plugin:
        executor: central_deployment_agent
        source: http://plugin.zip

node_types:
    test_type: {}

node_templates:
    node:
        type: test_type
        interfaces:
            cloudify.interfaces.lifecycle:
                start: plugin.tasks.start
