tosca_definitions_version: cloudify_dsl_1_3

description: >
    This plugin tries to use a blueprint from Cloudify catalog URL (`blueprint:...`)
    with a local `cfy blueprints validate`

imports:
  - ns--blueprint:test

